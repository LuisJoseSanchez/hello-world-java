name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build-container-image:
    runs-on: ubuntu-latest
    steps:
    - name: 'Checkout GitHub Action'
      uses: actions/checkout@main
      
    - name: 'Login via Azure CLI'
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}
    
    - name: 'Build and Push Image to ACR'
      uses: azure/docker-login@v1
      with:
        login-server: acrappsteer01.azurecr.io
        username: acrappsteer01
        password: T0Y2upicKm+Nh0Ejy01ihBDhAgwuc2xq
    - run: |
        docker build --tag acrappsteer01.azurecr.io/bookstoreapi:${{ github.sha }} .
        docker push acrappsteer01.azurecr.io/bookstoreapi:${{ github.sha }}
